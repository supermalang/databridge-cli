requests>=2.31.0
pyyaml>=6.0
python-dotenv>=1.0.0
pandas>=2.0.0
numpy>=1.25.0
openpyxl>=3.1.0
matplotlib>=3.8.0
squarify>=0.4.3
python-docx>=1.1.0
docxtpl>=0.16.0
click>=8.1.0
# Web server (FastAPI + uvicorn) — used by web/main.py and scripts/{dev,serve}.sh
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
aiofiles>=23.2.1
# Auth — OIDC login gate (BFF + encrypted cookie session)
authlib>=1.3.0
cryptography>=42.0.0
itsdangerous>=2.1.0
# Database (app state + optional export):
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
alembic>=1.13.0
# Object storage (Minio/S3)
boto3>=1.34.0
# Optional shared (cross-worker) backend for the perf cache (PERF-2). Imported
# lazily only when PERF_CACHE_URL / REDIS_URL is set; unset → in-process dict.
redis>=5.0.0
# pymysql>=1.1.0
# supabase>=2.0.0
# Uncomment for dot_map with OpenStreetMap basemap (basemap: true):
# contextily>=1.6.0
# pyproj>=3.6.0
# AI narrative generation (OpenAI-compatible: OpenAI, Azure, Groq, Mistral, Ollama):
openai>=1.0.0
# AI narrative generation (Anthropic Claude):
anthropic>=0.20.0
# Prompt management + LLM tracing (code targets the v4 SDK API):
langfuse>=4.0.0
# Test dependencies
pytest>=8.0
httpx>=0.27
# JSON Schema meta-schema validation (seed_prompts tests):
jsonschema>=4.0.0
