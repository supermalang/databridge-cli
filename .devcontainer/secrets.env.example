# Copy to secrets.env and fill in real values. secrets.env is gitignored.
# Format: KEY=value — no `export`, no surrounding quotes.

CLAUDE_CODE_OAUTH_TOKEN=sk-ant-oat01-your-token-here
# GH_TOKEN=ghp_your-token-here